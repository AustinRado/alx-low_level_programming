#include <stdio.h>
/**
 * main - Serves as the entry point for the program
 * main(void) - is a function that expects no arguments. returns an integer representing the application software status.
 * puts() function writes a string to stout up to but not including the null character. A newline character is appended to the output.
 * Return: 0 (Success)
 */
 int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
returns(0);
}
